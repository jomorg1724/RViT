# VDA16 cued versus opposite-corner attention

Cue S1 is fixed at the top-left with 100% displayed validity. The physical change is forced either at S1 or at S16 (bottom-right) on matched latent trial streams. Achieved spatial attention sums the image key and corresponding recurrent-memory key and averages over query tokens.

The focal comparison uses 30 degrees. See `data/summary.json` for paired t4 and t5 attention differences and the figures for behavior and spatial maps. This is one competence-gated checkpoint; evaluation-trial intervals are not training-seed uncertainty.
