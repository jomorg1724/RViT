# CLAIM_LEDGER — what the rebuilt paper says about each claim

This file is the rebuild's spine. One row per paper claim/assumption.
"Reviewer label" tracks the live verdict at `Critique/verdicts/`;
"Rebuilt strength" is the strongest form the rebuilt manuscript is
allowed to state; "Backing" lists the artifact(s) under `Rebuild/` that
back it.

> The reviewer is still running. Re-read the live verdict files at
> the top of every run and update the table below if a label moves.

Last reconciled with live ledger: **2026-05-31 (rb-051)** — no label drift (10/10 verdict labels still match the §3 table of `agents/paper_rebuilder_prompt.md` v0.2; only the §3 A6 entry remains stale, already flagged below). rb-051 was the **closed-form CF<1/2 boundary derivation increment (RB-024)** — the only C1 derivation thread still open and the rb-050 "next priority" recommendation. The new derivation `Rebuild/derivations/C1--cf-half-boundary.md` (~30 KB / 9 sections + verification + extensions) replaces the empirical `frac<0.6 = 22%` aggregate the §results-c1 manuscript section currently quotes with a **closed-form predicate the reader can compute pointwise** from the model primitives. Three pillars: (i) Proposition 2.1 (G_crit is r-independent — the criterion-only policies P3, P4 both hold α=1/N where the sensitivity collapse d'_c = d'_u = d'_base erases every r-dependence; verified at machine identity max|ΔG_crit|=0 across all 4,410 rb-003 rows, the same exact-equality recovery contract the C2 Proposition 4.1 ρ→0 limit licenses); (ii) Theorem 4.3 closed-form necessary condition (CF<1/2 is reachable at some r iff G_att^∞(V,v) > G_crit(V,v), with G_att^∞ the closed-form r→∞ asymptote at α=1; **variant A 0/105 reachable** so "variant A CF≥1/2 everywhere" is promoted from an empirical rb-003 observation to a theorem of the model under variant A's CR(V,v)=Vv+(1-V) scaling — the variant-A minimum margin G_att^∞−G_crit is strictly negative at every (V,v) with max margin −0.0034; **variant B 41/105 reachable** with five small-margin near-edge mismatches at (V,v) ∈ {(0.4000,5), (0.4375,5), (0.5500,4), (0.6625,3), (0.7375,2)} where the closed form predicts r_½ > 10, four of which the extended-grid sufficient threshold catches at r_*=30 and one (V=0.4375, margin +0.0005) at r_*>100); (iii) Theorem 5.1 closed-form sufficient condition (CF<1/2 if G_att^{α=1}(r;V,v) > G_crit(V,v), monotone in r, gives a closed-form upper bound r_*(V,v) on r_½(V,v); 0 violations across the 4,830-row extended sweep, soundness 1.0 within empirical range, coverage 0.9096 of empirical CF<0.5 cells, median per-cell gap r_*−r_½_emp = 0 — the α=1 specialisation is bit-tight on the median cell, capturing the same r-grid index as the unconstrained P1 optimum more than half the time). Companion verification script `Rebuild/derivations/verify_C1_cf_half/verify.py` produces `output.json` sha256 `b0b8ad5376e0b874982d97640639334112c6d1a396c1e6f8fd1e9ba09a61fe04` (deterministic across re-runs; ~1.4 s wall-clock; consumes rb-003 `results.json` sha256 `91fc4692…`). The derivation reuses the C2 boundary-configuration analysis (Eq. 1.3 of `Rebuild/derivations/C2--non-monotonic-vda-rho.md`) for the sensitivity collapse but is otherwise independent — no analogue exists in `Critique/derivations/C1--*` and the reviewer's verdict reports only empirical CF distributions, not a closed-form boundary characterisation. Two new propositions, two theorems, no manuscript edits yet — fold-in into `Rebuild/manuscript/sections/results.tex` §results-c1 and/or a new `Rebuild/manuscript/sections/appendix.tex` `sec:appendix-deriv-c1` subsection mirroring §appendix-deriv-c2's structure is a spawned follow-up (RB-056). 0 new bib entries; 0 new wiki stubs; `audit.py` not re-run (no wiki writes). Previous rb-050 reconcile (§methods bookend details) is preserved in the rb-050 BUILD_LOG entry and the methods.tex section file itself; rolled into "Previous reconcile" position below.

Previous reconcile (rb-050): **manuscript bookend increment (RB-050)** — drafting the §methods bookend — and made no claim-strength changes. The 5-subsection methods section (~250 LaTeX lines, ~1,700 words) replaces the rb-005 stub at `Rebuild/manuscript/sections/methods.tex` with a self-contained catalogue of the rebuild's simulation infrastructure paralleling the original paper's §3 methods. Five subsections: §8.1 the validated reference implementation `Rebuild/model/core.py` with the four extension axes A1 ($\corr$ channel via one-factor Gauss-Hermite reduction, FP-identity recovery at $\corr=0$, cites `eq:pnofa-indep` / `eq:pnofa-rho` / `eq:rho-zero-recovery` / `def:three-levers` / `sec:model-rho-channel` / `sec:model-upper-bound` / `sec:appendix-deriv-a1`), A3 (power-mean conservation family, byte-exact recovery at $p=1$, cites `eq:conservation-family` / `eq:beta-gamma-of-p` / `eq:a3d-hlp-kl` / `sec:appendix-deriv-a3` / `sec:extensions-a3`), A2 (per-location $\Rsens_i$ via `eq:d-prime-hetero` with byte-exact recovery under uniform $\Rsens_i$, cites `sec:extensions-a2`), A8 (full $\Nloc$-dim allocation simplex with $10^{-9}$ ULP recovery, cites `sec:extensions-a8`); §8.2 recovery contracts with all four sha256 digests in Table `tab:methods-recovery` (recovery_test `d3c62215…` for A1, conservation_family_test `f4f57a89…` for A3, heterogeneous_r_test `0486921f…` for A2, general_policy_test `883ea15a…` for A8); §8.3 simulation protocol (seeded grids, recovery test before sweep, canonical JSON output with sha256, captioned figures, backlog cross-reference) + Table `tab:methods-sims-summary` listing all nine wired sims with their output digest prefixes and manuscript cross-references (A1--rho-channel `b692c064…` / C1--cf-distribution `91fc4692…` / C2--vda-vs-r-vfamily `09ecef3c…` / C3--iso-vda-Vv `72820559…` / C4--anti-cue-inversion `6ad651d6…` / A1--vda-signflip-cellwise `489c7c25…` / A2--heterogeneous-r `22b183f9…` / A3--conservation-band `055bf4ec…` / A8--nd-uncued-sweep `beb2aa87…`); §8.4 the four derivations under `Rebuild/derivations/` (A1 $\corr$-channel rb-008; C2 $\rdagger(\val)$ + $\corr>0$ extension rb-014 → `prop:r-dagger-rho`; C4 anti-cue inversion rb-026 → `eq:value-weight` / `eq:r-inv`; A3 power-mean conservation rb-029 → `prop:r-dagger-invariance` / `cor:a3d-c5-invariance`); §8.5 reproducibility and the rebuild contract (simulate first / write second / no claim more strongly than its CLAIM_LEDGER row licenses / every quantitative claim pinned to a deterministic digest in `rebuilder_state.json`). Cross-references: ~50 labels (4 recovery digests, 9 sim digest prefixes, 11 section refs, 11 equation refs, 4 proposition/corollary/theorem refs, 1 definition ref, 11 figure refs, 7 table refs, 1 citation `Slepian1962`) — all verified extant in the manuscript via the pre-compile grep snapshot. One mid-build correction: first draft used `\citep{Slepian1962}` (natbib) but the manuscript loads no natbib — pass 1 fatal `! Undefined control sequence l.323 \citep`; fixed inline to `\cite{}` before pass 2. Build: 4-pass pdflatex+bibtex clean — pass 1 (after `\cite` fix) exit 0, 63 pages, 60+ undefined-reference warnings on first pass (every new label was new on pass 1 — normal); bibtex pass 0 new entries (the Slepian1962 citation is already wired from rb-008); passes 2/3/4 exit 0, 63 pages / 2,860,922 bytes, settled byte-identical pass 3 ↔ pass 4, 0 undefined references, 0 non-hyperref warnings (the persistent hyperref Unicode-in-PDF-string warnings on TOC bookmarks are inherited cosmetic warnings about Greek letters and math in subsection headers, not new and not semantic). PDF 63 pages / 2,860,922 bytes (was 59 / 2,815,939 at rb-049 = +4 pages, +44,983 bytes — the largest bookend yet because Table `tab:methods-sims-summary` and Table `tab:methods-recovery` together add full-width tabular blocks that the abstract/intro/limitations bookends did not). 0 new bib entries; 0 new wiki stubs; `audit.py` not re-run (no wiki writes). The rebuilt paper now has **all four bookends drafted and the manuscript is structurally complete for the first time** — abstract (rb-047) → intro (rb-049) → model (rb-009) → results × 4 (rb-006/rb-007/rb-011/rb-013) → extensions × 3 (rb-017/rb-022/rb-028) → limitations (rb-048) → **methods (rb-050)** → appendix × 3 (rb-018/rb-024/rb-046). Zero skeleton stubs remain; every section file is a self-contained, citation-resolved, recovery-pinned rebuild artefact. The remaining 14 queued tasks (RB-023, RB-024, RB-027, RB-028, RB-029, RB-031, RB-032, RB-036, RB-037, RB-039, RB-040, RB-043, RB-044, RB-045) are all low-priority sharpening passes (closed-form boundary derivations, variant-B re-evaluations, finer-grid sims, dormant-cell amplification closeup, Slepian-gradient analytic locus). Previous rb-049 reconcile (§intro details) is preserved in the rb-049 BUILD_LOG entry and the intro.tex section file itself; rolled out of this reconcile paragraph since the bookend quartet is now also referenced from the §methods voice anchors.

Previous reconcile (rb-049): manuscript bookend increment (RB-049) — drafting the §intro bookend — and made no claim-strength changes. The 6-paragraph intro (≈165 LaTeX lines, ~1,100 words) replaces the rb-005 stub at `Rebuild/manuscript/sections/intro.tex` with a self-contained introduction that articulates the §3.3 unifying reframe up-front: paragraph 1 states the original VDA question verbatim in spirit and names the three available levers (criterion, sensitivity, decorrelation) at the top; paragraph 2 articulates the corrective voice by quoting the four inherited overstatements (`[0.60, 0.96]` floor → `[0.30, 1.00]` median 0.76; "negligible regardless of other parameters" at `\valid ≥ 0.95` only; "no inversion" true cued but not anti-cue; A1 "upper bound on VDA" sign-ambiguous) and pivoting to distributional/graded/conditional by default; paragraph 3 introduces the missing third lever (citing `CohenMaunsell2009`, `RuffCohen2016`, `Srinath2021` already wired from rb-009/rb-011/rb-024) with the one-factor Gauss-Hermite reduction (Eq. eq:pnofa-rho), the FP-identity ρ→0 recovery (Eq. eq:rho-zero-recovery), the three-lever Defn def:three-levers, and the A1 §model-upper-bound retraction with the cell-wise crossover at $\Rsens \approx 0.79$ (Section sec:model-upper-bound, Table tab:a1cw-summary); paragraph 4 previews the four C-row headline results at CLAIM_LEDGER-licensed strength (C1 distributional `sec:results-c1` / `tab:cf-distribution`; C2 closed-form `eq:r-dagger` + ρ extension `prop:r-dagger-rho` + conservation-form invariance `prop:r-dagger-invariance`; C3 graded contour band `sec:results-c3` / `tab:c3-highV-probe` with hierarchical `\valid` thresholds; C4 conditional theorem `eq:value-weight` + anti-cue inversion 36.1% prediction at `tab:c4-anticue`; C5 universal real-number identity `prop:c5-realnumber` / `cor:a3d-c5-invariance`); paragraph 5 previews the three extension levers (A2 bounded heterogeneity `tab:a2-rb021-summary`; A3 power-mean conservation family `eq:conservation-family` / `eq:beta-gamma-of-p` / `thm:delta-cf-monotone`; A8 N-dim policy with new conditional binding at 32% ρ-amplification) + A6 deferral explicitly held at WEAKLY-SUPPORTED (`sec:limitations-a6`); paragraph 6 lays out the manuscript structure (model → results → extensions → limitations → appendix) and closes with the §3.3 voice clause (every claim backed by sim+recovered limit+hash+figure; every proposition derived; nothing stated more strongly than its CLAIM_LEDGER row licenses). Cross-references: 25 labels — all verified extant before compile via the grep snapshot. Build 4-pass pdflatex clean (pass 1 with one `! Undefined control sequence l.70 \citep` fatal hit and fixed inline to `\cite{}` before re-running; bibtex no-op no new keys; passes 2-4 settled byte-identical, 0 undefined refs, 0 new warnings). PDF 59 pages / 2,815,939 bytes (was 58 / 2,807,597 at rb-048 = +1 page, +8,342 bytes). 0 new bib entries; 0 new wiki stubs; `audit.py` not re-run (no wiki writes). The rebuilt paper now has all three drafted bookends (abstract → intro → limitations) for the first time: abstract → **intro** → model → results × 4 → extensions × 3 → limitations → methods stub → appendix × 3. Only the §methods bookend remains as a structural increment; after methods the manuscript is structurally complete and the remaining work is sharpening passes only. Previous rb-048 reconcile (§limitations details) is preserved in the rb-048 BUILD_LOG entry and the limitations.tex section file itself; rolled out of this reconcile paragraph since the bookend trio is now also referenced from the §intro voice anchors.

Previous reconcile (rb-048): manuscript bookend increment (RB-048) — drafting the §limitations bookend — and made no claim-strength changes. The 6-subsection limitations section (≈170 LaTeX lines, ~1,400 words) replaces the rb-005 stub at `Rebuild/manuscript/sections/limitations.tex` with a navigable Section~7 that collects the explicit scope deferrals every body section has already flagged: §7.1 conservation rule as a power-mean parameter (cites Eq. eq:conservation-family / eq:beta-gamma-of-p / eq:a3d-power-mean and the open $\Delta\CF \le 0$ closed-form conjecture in §appendix-deriv-a3 with its anchor at Prop prop:a3d-rp3-invariance, Eq. eq:a3d-chain-rule, Eq. eq:a3d-dprime-of-p); §7.2 heterogeneity bounded not abolished (A2 between-preparation reading cites Eq. eq:d-prime-hetero / Table tab:a2-rb021-summary / §extensions-a2 / mcadams_maunsell1999_v4_tuning / reynolds_heeger2009_normalization / carrasco2011_visual_attention_25y; A8 new conditional cites §extensions-a8 Finding F2 with quoted $\Delta R$ numbers); §7.3 decision-noise lever deferred not denied (A6 explicitly held at WEAKLY-SUPPORTED until the live verdict moves, with the would-be fourth-lever framing made explicit by symmetry with the $\corr$ channel); §7.4 assumptions retained as explicit scope (A4 no learning / A5 transfer-function family $h \in \{a, \sqrt{a}, a^{0.3}, a^{2}\}$ with Defn def:three-levers / Eq. eq:r-dagger / Prop prop:r-dagger-invariance stated in generic $f$ / A7 reward variants A vs B with variant-B sensitivity citations Table tab:cf-distribution / tab:cf-quadrants / tab:a2-rb021-summary); §7.5 equicorrelation / $\corr$-envelope / variant-B caveats / grid-sharpening follow-ups (cites Eq. eq:equicorr-cov / eq:pnofa-rho / Prop prop:r-dagger-rho / Table tab:r-dagger-rho-drift / RuffCohen2016 / Srinath2021 / CohenMaunsell2009 $r_{SC} \approx 0.2$ anchor + the queued $C_3$ / $C_4$ / A8 grid-sharpening sims + the closed-form $\CF < 0.5$ boundary derivation + the dormant-cell amplification closeup + the Slepian-style analytic locus for cell-wise $\partial \VDA / \partial \corr$); §7.6 what the rebuild does not claim (no neural-implementation claim; no empirical-$\corr$ prediction beyond the CohenMaunsell anchor; no observer-deviation prediction; no attention-dynamics claim — explicit non-claims, matching mission §3.3 voice that the rebuilt paper is a "mathematical description"). Cross-references: 25 section / equation / proposition / theorem / table / citation labels (all verified extant in the manuscript, no new bib entries needed — every cited reference was already wired). Build 3-pass pdflatex clean (pass 1 → pass 2 (one forward reference `sec:limitations-followups` resolved on pass 2) → pass 3 settled byte-identical to pass 2, 0 undefined refs, 0 new warnings). PDF 58 pages / 2,807,597 bytes (was 55 / 2,792,842 at rb-047 = +3 pages, +14,755 bytes). 0 new bib entries; 0 new wiki stubs; `audit.py` not re-run (no wiki writes). The rebuilt paper has both bookends drafted for the first time: abstract → intro stub → model → results × 4 → extensions × 3 → **limitations** → methods stub → appendix × 3. The two remaining skeleton stubs (intro, methods) are the final bookend follow-ups; the §scope paragraphs scattered across body sections are now all collected in §limitations and can be cited by section rather than re-stated inline if a later increment tightens them. Previous rb-047 reconcile (abstract details) is preserved in the rb-047 BUILD_LOG entry and the abstract.tex section file itself; rolled out of this reconcile paragraph since the bookend pair is now also referenced from the §limitations §7.4–§7.5 voice anchors.

Previous reconcile (rb-046): no label drift (10/10 verdict labels still match the §3 table of `agents/paper_rebuilder_prompt.md` v0.2; only the §3 A6 entry remains stale, already flagged below). rb-046 was the **§appendix-deriv-a3 manuscript fold-in (RB-046, prereq RB-033 done at rb-029)**, the natural follow-up to the rb-029 A3 power-mean derivation. The fold-in promotes the rb-029 derivation file (`Rebuild/derivations/A3--power-mean-conservation.md`, ~50 KB / 1026 lines) into a self-contained manuscript appendix subsection mirroring §appendix-deriv-c2's structure (rb-024 fold-in pattern). Contents of the new `Rebuild/manuscript/sections/appendix.tex` `sec:appendix-deriv-a3` subsection (≈220 LaTeX lines / 27 KB added to the PDF, 55 pages / 2,790,640 bytes — was 2,763,376 at rb-028): (i) the power-mean family Eq. eq:a3d-power-mean and the boxed closed-form weights $\cost(\Rsens; p) = (2/(\Rsens^p+1))^{1/p}, \benefit = \Rsens\cost$ (Eq. eq:a3d-closed-form-weights, mirror of Eq. eq:beta-gamma-of-p of §extensions-A3); (ii) HLP power-mean monotonicity in pointwise KL-divergence form $\partial \ln \cost / \partial p = -(1/p^2) \DKL(\mathrm{Bern}(\theta_p) \| \mathrm{Bern}(1/2))$ (Eq. eq:a3d-hlp-kl, boxed) + sign-of-gradient corollary Eq. eq:a3d-mono-sign + continuity through $p = 0$ via the $-(\ln \Rsens)^2/8$ limit; (iii) Proposition prop:a3d-symmetric-corner (symmetric-corner identity $\benefit(1; p) = \cost(1; p) = 1$ at every $p$, full proof from Eq. eq:a3d-closed-form-weights) + Corollary cor:a3d-c5-invariance (C5 conservation-form-invariance, full proof from the $\dprime$-map at $\Rsens=1$); (iv) **full proof of Proposition prop:r-dagger-invariance** in three steps (sensitivities $p$-independent at $\alpha=1/N$ via the vanishing bracket $\dprimemax f(1/N) - \dprime_{\mathrm{base}}$; P3-optimal criteria $p$-independent by functional composition; $K_c, K_u$ ratio $p$-independent), promoting the rb-017 §extensions-A3 sketch to a structural derivation; (v) the d'-channel chain rule Eq. eq:a3d-chain-rule / eq:a3d-dprime-of-p establishing the opposite-sign of $\partial \dprime_c/\partial p$ vs $\partial \dprime_u/\partial p$ and Proposition prop:a3d-rp3-invariance ($\partial \Rpthree/\partial p \equiv 0$ at $\alpha = 1/N$, full proof) as the one analytic full-strength substatement, with the uniform $|\partial_p R_{\mathrm{P4}}| \le |\partial_p R_{\mathrm{P1}}|$ inequality declared open. Cross-references updated: extensions.tex §extensions-A3 intro paragraph (rb-017 line 53 "queued companion increment RB-033" → discharged citation of the new appendix); the in-section proof-sketch of prop:r-dagger-invariance now closes with a one-paragraph pointer to the full proof in Appendix~\ref{sec:appendix-deriv-a3}; the Theorem thm:delta-cf-monotone parenthetical (rb-017 line 350 "queued companion increment RB-033 is the natural home for one" → discharged with chain-rule cite + Prop prop:a3d-rp3-invariance + open-conjecture statement); the §extensions-A3 scope paragraph item (ii) (rb-017 lines 417-418 "Appendix~\ref{sec:appendix-deriv-a3} (RB-033)" → discharged); the §appendix-c5 conservation-form-invariance paragraph picks up an additional formal cross-reference to Prop prop:a3d-symmetric-corner / Cor cor:a3d-c5-invariance (the existing inline proof at §appendix-c5 line 481-497 is preserved verbatim). One new macro added to `main.tex`: `\DKL = D_{\mathrm{KL}}`. Build 4-pass clean: pass 1 (after the \DKL macro fix and the \Pthree/\Pone/\Pfour/\Ppolicy/\Ethree/\Rpi macro-stub fixes — replaced inline with `\textrm{P3}` / `\textrm{P1}` / `\textrm{P4}` / `\textrm{P}` / `\Rpthree` / `R_{\mathrm{P}_i}` since the manuscript convention is plain-text policy labels, not macros) → bibtex no-op (0 new bib entries; HLP1934 already wired at rb-017) → passes 2-4 (settled, 0 undefined refs, 0 non-hyperref warnings). PDF 55 pages / 2,790,640 bytes (vs rb-028 2,763,376 = +27,264 bytes for the new subsection — appendix subsection without figures, comparable to the rb-024 §appendix-deriv-c2 fold-in pattern). **A3 row backing column extended in full**: now wired across model (rb-015), sim (rb-016), manuscript-extensions (rb-017), derivation (rb-029, RB-033), AND manuscript-appendix (rb-046, RB-046) — the A3 thread is now fully discharged across all four output kinds for the first time of any claim in the rebuild. The C5 row picks up a tighter cross-reference (Prop prop:a3d-symmetric-corner / Cor cor:a3d-c5-invariance from the new appendix). No model edits; no new sims; no new bib entries; no new wiki stubs; audit.py not re-run. Previous rb-029 reconcile (derivation file `A3--power-mean-conservation.md` content listing + KL closed form Eq. 3.3 numerical cross-check + Proposition 5.1 / 4.1 / Corollary 4.2 / chain-rule sign analysis details) is preserved in the rb-029 BUILD_LOG entry and the derivation file itself; rolled out of this reconcile paragraph since the content is now also folded into the manuscript appendix above.

## Headline claims

| id | reviewer label (live) | rebuilt strength | backing |
| --- | --- | --- | --- |
| C1 — criterion captures the bulk of value-related reward | CONTESTED | **Distributional / central-tendency**: criterion shifts are typically the dominant lever (median CF = 0.7552 variant A, 0.7682 variant B); CF concentrated in [0.30, 1.00] across the 4,410-cell sweep with `frac<0.6 = 7% (A) / 20% (B)`. Headline interval `[0.60, 0.96]` retracted on both ends: variant-A strict min = 0.5587; variant-B 8% of cells below 0.50. Adding ρ = 0.2 (A1 sensitivity) triples variant-A `frac<0.6` (0.07 → 0.22) and drops the strict min below 0.50; cell-wise `CF(ρ=0.2) ≤ CF(ρ=0)` in 84% of variant-A cells, 64% of variant-B (variant B more mixed, consistent with rb-002 headline-cell flat-in-ρ caveat). **Conservation-family band added (rb-016, RB-019):** swapping the inherited additive rule for the multiplicative endpoint (p = 1 → p = 0) doubles the `frac<0.5` fraction from 4.0% to 8.3% (combined), drives 191 cells from CF ≥ 0.5 to CF < 0.5 with 0 reverse flips, deepens the variant-B min from 0.3040 to 0.2309, and produces ΔCF ≤ 0 in *every* valid cell. The median CF moves only marginally (variant A −0.0012; variant B −0.0042): central tendency robust to conservation form, tail shape is not. Cell-wise ΔCF ≤ 0 monotonicity is itself a finding not stated in the inherited paper. | `Rebuild/sims/C1--cf-distribution/` (rb-003, 4,410 cells × {0, 0.2}, sha256 `91fc4692...`, three figures: cf_histogram.png, cf_heatmap.png, cf_curves.png; recovery vs reviewer CR-002 at `max\|ΔCF\|=1.47e-6`); **conservation-family band: `Rebuild/sims/A3--conservation-band/` (rb-016, RB-019, sha256 `055bf4ec…`) — Block B 4,410-cell × 2-p CF distribution + per-cell ΔCF + flip-count tally exactly reproducing the reviewer's verdict-text headline (191 flips, 0 reverse, min CF× = 0.231); figures cf_histogram_pfamily.png and delta_cf_distribution.png**; **manuscript drafted: `Rebuild/manuscript/sections/results.tex` §results-c1 (rb-007, RB-009): CF definition (eq:cf-def), 4 numbered tables (cf-distribution, cf-quadrants, cf-rho-sensitivity, cf-delta-distribution), 3 figures `\\includegraphics`d (cf_histogram, cf_heatmap, cf_curves); MullerFindlay1987 citation added; PDF 12 pages / 897 KB** (the rb-016 conservation-family band will land as a short paragraph in the §extensions-A3 increment RB-034, cross-referenced from §results-c1); **closed-form CF<1/2 boundary derivation: `Rebuild/derivations/C1--cf-half-boundary.md` (rb-051, RB-024) + companion `verify_C1_cf_half/output.json` sha256 `b0b8ad53…` — Proposition 2.1 (G_crit is r-independent: verified at machine identity max\|Δ\|=0 across all 4,410 rb-003 rows), Theorem 4.3 (closed-form necessary condition G_att^∞(V,v) > G_crit(V,v): variant A 0/105 reachable so "CF≥1/2 everywhere in variant A" is now a theorem of the model under variant A's CR scaling; variant B 41/105 reachable; 5 small-margin near-edge mismatches with predicted r_½ > 10), Theorem 5.1 (closed-form sufficient condition G_att^{α=1}(r;V,v) > G_crit(V,v): 0 violations, soundness 1.0 within empirical range, coverage 0.91 of empirical CF<0.5 cells, median per-cell gap r_*−r_½_emp = 0). |
| C2 — VDA is non-monotonic in r | CONFIRMED-UNDER-ATTACK | **Confirmed**, strengthened by reporting the closed-form escape threshold `r†(v) = K_u(v) / [(N-1) · K_c(v)]` and its v-family numerics. At the headline cell (V=0.5, N=4, variant A): r†(v) monotone-decreases from 0.343 at v=1 to 0.016 at v=10. The empirical peak r* lies in [0.36, 0.50] across v ∈ {2..10}, always above r†(v) (gap +0.28 to +0.35), confirming the §2.3 prediction that VDA opens up where P1 has escaped but P2 has not. Peak VDA grows monotonically with v (0.012 → 0.183 across the v-family). Adding ρ=0.2 suppresses the peak at low v (Δpeak −0.002 at v=2, −0.003 at v=5) and amplifies at v=10 (+0.001) — the A1 sign-flip generalised across the v-family. **Strengthened (rb-016, RB-019):** r†(v) is *conservation-form-invariant* — a free theorem. At α = 1/N the perturbation bracket d_max · f(1/N) − d_base = 0, so d_c = d_u = d_base regardless of (r, p); the criterion-optimum (c_c*, c_u*) and the gradients K_c(v), K_u(v) are therefore p-independent and r†(v) = K_u(v) / [(N-1) · K_c(v)] is the same closed-form number for every conservation order. Hard-verified numerically: K_c, K_u, r_dagger all differ by *exactly 0.0* across p ∈ {0, 0.5, 1.0} at every v ∈ {2, 3, 5, 8, 10}. The empirical band on peak (r*, VDA*) across p is reported as a sensitivity (peak r* at v=5 shifts 0.3758 → 0.3548 from p=1 to p=0; peak VDA* shifts 0.0830 → 0.0951, a ~14% widening of the conservation envelope). **Extended to ρ > 0 (rb-023, RB-026):** the ρ-aware closed form `r†(v; ρ) = K_u(v; ρ) / [(N-1) · K_c(v; ρ)]` (Proposition 4.1 of `Rebuild/derivations/C2--non-monotonic-vda-rho.md`) replaces the inherited $I_c^{0}, I_u^{0}$ d-gradient products by the equicorrelated Gauss–Hermite quadratures $I_c(b_c, b_u; \rho, N), I_u(b_c, b_u; \rho, N)$ from `Rebuild/model/core.py:p_no_fa_grid` (Eqs. 3.4–3.5), evaluated at the ρ-aware **asymmetric P3 criterion optimum** $(c^{\star}_c(\rho), c^{\star}_u(\rho))$. ρ → 0 recovery: structural (Eq. 4.2 collapses I_c, I_u term-by-term) + numerical (max\|Δ\| ≤ 5e-4 vs reviewer/rb-006 on C_GRID-quantised criteria, binary at v=2). At headline cell: $\Delta r^{\dagger}(v) = r^{\dagger}(v; 0.2) - r^{\dagger}(v; 0) > 0$ at **every** $v \in \{1,2,3,5,8,10\}$ (escape threshold drifts upward under correlation across the entire $v$ family; +3.0% at v=1 → +29.7% at v=8); the closed-form $\Delta r^{\dagger}$ sign matches the rb-006 empirical $\Delta r^{\star}$ sign at 5/5 tested $v \ne 1$. Boundary FD sign-flip: 6/6 confirmed at $r = r^{\dagger}(v;\rho) \pm 0.05$ for $(v, \rho) \in \{1,2,3\} \times \{0, 0.2\}$. The closed form makes A1's *channel (b) concentration-cost relaxation* (cf. `Rebuild/derivations/A1--rho-channel.md` §4.2) **analytic** — what was an empirical sign-flip observation in rb-002 / rb-004 / rb-010 now has a closed-form lower-edge predictor. | `Rebuild/sims/C2--vda-vs-r-vfamily/` (rb-004, 84 r × 5 v × 2 ρ on the rebuilt model, sha256 `09ecef3c...`, figures vda_curves_vfamily.png and r_dagger_vs_v.png; recovery vs rb-002 at all 4 pinned r values to ≤ 6e-6); **conservation-family band: `Rebuild/sims/A3--conservation-band/` (rb-016, RB-019, sha256 `055bf4ec…`) — Block A's r†(v) p-invariance theorem to FP identity, peak-(r*, VDA*) band table across p ∈ {0, 0.5, 1.0} for v ∈ {2, 3, 5, 8, 10}; figures vda_curves_pfamily_v5.png, vda_peak_band.png**; **derivation drafted: `Rebuild/derivations/C2--non-monotonic-vda-rho.md` (rb-023, RB-026): rebuild-voice re-derivation at ρ=0 (Proposition 2.1) + ρ>0 extension (Proposition 4.1, boxed) + ρ→0 recovery (structural Eq. 4.2 + numerical §5.1 ≤ 5e-4 on C_GRID, binary at v=2) + drift sign-match 5/5 + boundary FD 6/6; companion `verify_C2_rho/verify.py` deterministic, output.json sha256 `ddbd3988e0253fde2cfae5906ca2749cda872d1382055b3d243b4b7c6a0678dc`**; **manuscript drafted: `Rebuild/manuscript/sections/results.tex` §results-c2 (rb-006, RB-010): closed-form r† display equation, v-family table, peak-vs-threshold table, A1 sensitivity table, both figures `\\includegraphics`d; PDF 7 pages / 542 KB**; **appendix subsection drafted: `Rebuild/manuscript/sections/appendix.tex` §appendix-deriv-c2 (rb-024, RB-038): the rb-023 ρ-aware closed form folded into the manuscript as a self-contained subsection — Proposition prop:r-dagger-rho (boxed Eq. eq:r-dagger-rho), ρ-aware d-gradient integrals (Eqs. eq:c2-Ic / eq:c2-Iu), boundary FOC (Eq. eq:c2-rho-foc), structural ρ→0 recovery to §results-c2 Eqs. eq:r-dagger / eq:K-c / eq:K-u, drift Table tab:r-dagger-rho-drift (5/5 sign-match against tab:rho-sensitivity at v ≠ 1), boundary-FD 6/6 witness, mechanistic mirror to §appendix-deriv-a1 Slepian monotonicity (the appendix subsection pins the lower-edge r† component of dVDA/dρ; §appendix-deriv-a1 pins the CF-versus-ρ side). §results-c2 forward-reference paragraph rewritten to cite the appendix subsection directly. 43 pages / 2,413,318 bytes (was 40/2,379,430 at rb-022 = +3 pages, +33,888 bytes).** (The rb-016 p-invariance one-liner and conservation-band sensitivity table are queued as a small §results-c2 update under the §extensions-A3 increment RB-034.) |
| C3 — narrow VDA regime | CONTESTED | **Graded/quantitative**: VDA concentrates at low V, high v, moderate-low r; the §5.2 categorical experimental-design claim ("negligible VDA *regardless of other parameters*" at high V) is retracted in favor of a contour-band statement. Quantitative bound from rb-010 across 3,534 cells (variant A, `N=4, d'_max=2, f0=0.5, h=sqrt`): peak VDA = `0.173` (`r=0.3, ρ=0`), `0.157` (`r=1, ρ=0`), `0.062` (`r=3, ρ=0`); median VDA `≤ 0.007` in every panel — most of `(V, v)` carries near-zero VDA. The §5.2 "high-V negligible" threshold conditions: at `V ≥ 0.95` peak VDA is at the grid floor (`≤ 1e-5`) for every `r ∈ {0.3, 1, 3}` and `ρ ∈ {0, 0.2}` — categorical claim survives; at `V ≥ 0.80` it survives at `ρ=0` but admits a small `ρ`-conditional signal at `ρ=0.2` (max `0.0032` at `r=3`); at `V ≥ 0.60` it fails (peak VDA `0.164` at `r=0.3, ρ=0.2`). | `Rebuild/sims/C3--iso-vda-Vv/` (rb-010, 3,534 cells, sha256 `72820559...`, three figures: iso_vda_contours.png 2×3 grid, vda_at_high_V.png 1×3 strata, iso_vda_drho.png 1×3 sign-flip; recovery at rb-006 anchor `\|Δ\|=1.27e-7`); **manuscript drafted: `Rebuild/manuscript/sections/results.tex` §results-c3 (rb-011, RB-011): claim-restatement paragraph, sweep design paragraph, three numbered tables (tab:c3-marginals = per-panel VDA marginals sourced from `summary.r=<r>__rho=<rho>`; tab:c3-highV-probe = 4×3 high-V probe sourced from `high_V_probe.V=<V>.r=<r>__rho=<rho>.max_VDA`; tab:c3-sign-flip = 3-row (V,v)-plane sign-flip table sourced from `rho_sensitivity.r=<r>`), three figures `\\includegraphics`d (iso_vda_contours, vda_at_high_V, iso_vda_drho), and the boxed §5.2-replacement blockquote ("V ≥ 0.95 unconditionally; V ≥ 0.8 if r_SC ≤ 0.2; inherited 0.75 threshold too permissive — at V ∈ [0.6, 0.8) cost-dominant regime admits peak VDA ≈ 0.16"). A1 sign-flip generalisation across (V, v) reported as cross-axis corroboration (no A1 strength change). Cites CohenMaunsell2009; PDF 21 pages / 1570450 bytes.** |
| C4 — no inversion of optimal attention | CONFIRMED-CONDITIONAL | **Conditional theorem** with explicit condition `V ≥ 1/N` (sharp form `V ≥ 1/[(N-1)v+1]`; `1/N` is the worst-case recovered at `v=1`). Across the 4,410-cell primary sweep at `N=4`: `α*_global ≥ 1/N` at every cell, robust under A1 (ρ=0.2 leaves Step B 0 inversions and Step D 0 cued-region inversions intact). The mechanism is the **location-count asymmetry** combined with the **value-weight inequality `w_c ≥ w_u`** (derivation §6); the paper's "regardless of `r`" wording is correct as a global claim but incorrect as a local derivative statement — the left one-sided derivative at α=1/N has a closed-form sign-flip at `r†_inv = (N-1) A_0/B_0`, which falls inside `[0.1, 10]` in 48.6% of cells at ρ=0 and 52.4% at ρ=0.2 (rb-012 Step A vs reviewer's 49.0% at ρ=0; recovery PASS). At the symmetric corner `(V=1/N, v=1)`: `r†_inv = 1` exactly, independent of N, variant, and ρ (formal proof: Proposition 5.1, rb-014). **Anti-cue inversion (V < 1/N) added as a new falsifiable prediction of the rebuilt model**: at `N=4` (the paper's primary topology; reviewer's CR-004 tested only `N=2`), 36.1% of probed `V ∈ {0.05, 0.10, 0.15, 0.20}` × `v ∈ {1, 3, 5}` × `r ∈ {0.1..10}` cells exhibit `α*_global < 1/N` at ρ=0 (34.7% at ρ=0.2). Inversion incidence is r-stratified (50% at r ∈ {1, 3, 5}, 33% at r=10, 8% at r=0.1); the v-stratification follows the sharp boundary `V < 1/[(N-1)v+1]` (at v=5, N=4 inversion is confined to V < 1/16 ≈ 0.0625, well below the universal V=1/N=0.25 bound). A1 (ρ) does not abolish the inversion regime: ρ=0.2 shifts `r†_inv` quantitatively (median by 13–21%) but leaves the qualitative inversion locus and incidence essentially unchanged (25 vs 26 anti-cue inversions). | `Rebuild/sims/C4--anti-cue-inversion/` (rb-012, RB-008, sha256 `6ad651d6…`, 17.4 s wall-clock; three figures: er_vs_alpha_anticue.png, alpha_star_V_r_map.png, r_inv_closed_form.png; closed-form `r†_inv(V, v, N, CR, ρ)` extended to ρ>0 via the one-factor GH-64 quadrature `Rebuild/model/core.py` already uses for `P_no-fa(ρ)`; recovery #1 vs reviewer derivation §4 49% in [0.1, 10] → 48.6% (Δ 0.4 pp, PASS); recovery #2 vs reviewer derivation §5 Step C(i) table at `V=0.25, v=1, N=4, r ∈ {0.1, 1.0, 1.585, 2.512, 3.981, 10.0}` max |Δα|=0 / max |ΔR|=3e-6 (PASS both tols)). **Manuscript drafted: `Rebuild/manuscript/sections/results.tex` §results-c4 (rb-013, RB-012): claim-restatement paragraph (live verdict CONTESTED → CONFIRMED-CONDITIONAL with the explicit `V ≥ 1/N` condition, the bimodality of the boundary picture, and the anti-cue inversion as the new falsifiable prediction); mechanism paragraph with the value-weight inequality (eq:value-weight); closed-form local threshold paragraph with the boundary left-derivative display (eq:left-derivative), the `r†_inv = (N-1) A_0/B_0` definition (eq:r-inv), and the symmetric-corner identity (eq:r-inv-corner) — all three derived in the rebuild's voice in-section, with the formal derivation increment RB-030 queued separately; numerical-tally table tab:c4-rstar-tally (4 (variant, ρ) panels, recovers reviewer 49% to 48.6% at ρ=0); Step B primary-sweep probes table tab:c4-stepB (6 of 12 probes, 0 inversions); anti-cue incidence table tab:c4-anticue (r/v/V stratifiers at ρ=0, 36.1% total); A1 cross-axis sensitivity paragraph (qualitative invariance under ρ=0.2 across all four steps); behavioural-literature paragraph (Wang-Theeuwes 2018, Wang-Samara-Theeuwes 2019, Kong-Li-Wang-Theeuwes 2020, Failing-Theeuwes 2018, Hickey 2010, Posner 1980 — alignment with the live verdict V0.2 evidence dossier); scope limitations paragraph (variant B anti-cue → RB-031, V-grid sharpening → RB-032, formal derivation → RB-030); reproducibility paragraph (sha256 `6ad651d6…`, both recovery tests cited verbatim, symmetric-corner identity recovered to floating-point identity, value-blind sanity check at v=1 boundary noted). 3 figures `\includegraphics`d (`r_inv_closed_form.png`, `er_vs_alpha_anticue.png`, `alpha_star_V_r_map.png`) copied from `Rebuild/sims/C4--anti-cue-inversion/output/figures/` to `Rebuild/manuscript/figures/`. 6 new bib entries added to refs.bib (Wang-Theeuwes 2018; Wang-Samara-Theeuwes 2019; Kong-Li-Wang-Theeuwes 2020; Failing-Theeuwes 2018; Hickey 2010; Posner 1980) — first four wired to existing research_db stubs, Wang-Samara-Theeuwes 2019 and Kong et al. 2020 cited by full bibliographic reference per reviewer's V0.2 evidence dossier (research_db stubs queued under reviewer CR-035/CR-037). Build: 4-pass pdflatex+bibtex clean (pass 1 → bibtex (6 new entries) → pass 2 (undefined refs cleared) → pass 3 (settled); 0 undefined references in final pass); 27 pages / 1,814,626 bytes (was 21/1,570,450 at rb-011 = +6 pages, +244 KB).** **Formal appendix-derivation drafted: `Rebuild/derivations/C4--anti-cue-inversion.md` (rb-014, RB-030): independent re-derivation in the rebuild's voice (not a copy of `Critique/derivations/C4--no-inversion.md`) — ~42 KB, 9 sections + Verification block. §1 setup + inversion-branch kink (Eqs. 1.5–1.6 from β/γ swap); §2 boundary partials A_0, B_0 (Eqs. 2.1–2.3) and one-sided derivatives (Eqs. 2.4–2.5); §3 closed-form `r†_inv = (N-1) A_0/B_0` Eq. 3.3 with Proposition 3.1 (boundary inversion threshold) + structural mirror to C2's r†(v) §3.1; §4 ρ-extension via one-factor GH quadrature (Eqs. 4.1–4.4) + explicit ρ→0 collapse to inherited closed form (Eqs. 4.5–4.8) — same machinery as `Rebuild/derivations/A1--rho-channel.md` §2; §5 **Proposition 5.1 (symmetric-corner identity) `r†_inv(1/N, 1, N, CR, ρ) = 1`**, formal proof from FOC symmetry (Eqs. 5.2–5.8) — ρ cancels exactly through I(ρ) common to numerator and denominator, N cancels exactly through B_0 = (N-1) A_0; §6 value-weight inequality `w_c ≥ w_u ⟺ V ≥ 1/[(N-1)v+1]` (Eq. 6.2) + location-count asymmetry §6.2 + **Theorem 6.1 (global no-inversion, conditional)** §6.3 with empirical anchor (rb-012 Step B 0/12 global inversions); anti-cue prediction §6.4 derived from the (6.4) flipped value-weight inequality; §7 simulation attribution table mapping each derivation equation to its `run.py` line range and `results.json` block key, sha256 `6ad651d6…`, plus three recovery records (#1 ρ=0 vs reviewer §4, #2 vs reviewer §5 Step C(i), #3 symmetric-corner identity to FP identity under GH quadrature in all four (variant, ρ) panels); §8 scope (local statement only; equicorrelated noise only; additive A3 only; observational anchor for Theorem 6.1; variant A only for anti-cue); §9 references (inherited paper, reviewer's parallel derivation by full path, companion sim, manuscript §results-c4, A1 derivation cross-reference, Slepian 1962 + Tong 1990 by full bibliographic reference — same math-methods gap as A1 derivation). Verification: ρ→0 collapse verified by inspection; symmetric-corner identity verified algebraically AND numerically (1.0000 to FP identity in 4 panels); 48.6% vs 49.0% recovery PASS; one-sided derivatives cross-check at corner yields exact sign-flip at r=1=r†_inv. No new wiki stubs (math-methods gap inherited); no audit run.** |
| C5 — symmetric recovery at `r=1` | CONFIRMED-UNDER-ATTACK | **Confirmed** as an appendix consistency result; "machine precision" universally, "literal 0.0" configuration-specific; `r=1` is the smooth centre of the family, not a knife-edge. **Strengthened (rb-015):** the result is *conservation-form-invariant by construction* — at $r=1$, $\beta(1, p) = \gamma(1, p) = 1$ for every conservation order $p$ (any $M_p$ on $(\beta, \gamma)$ with $\beta = \gamma$ gives $\beta = \gamma = 1$), so the symmetric recovery survives swapping additive for multiplicative or any intermediate conservation rule without re-derivation. Verified numerically: `policies(p=0, r=1) == policies(p=1, r=1)` to floating-point identity. | recovery contract framework reused in `model/tests/test_recovery.py` (rb-001); conservation-form-invariance verified in `model/tests/test_conservation_family.py` (rb-015) — `test_symmetric_corner_invariant` PASS binary, `test_p0_symmetric_corner_identity` PASS to floating-point identity; **manuscript drafted: `Rebuild/manuscript/sections/appendix.tex` §appendix-C5 (rb-018, RB-013): claim-restatement paragraph at defensible CONFIRMED-UNDER-ATTACK strength (universal real-number identity; bit-exact $0.0$ at validation config only; $\Rsens = 1$ smooth centre, not knife-edge); **Proposition prop:c5-realnumber (real-number recovery at $\Rsens=1$)** — universal claim that $\beta(1)=\gamma(1)=1$ forces $d_c=d_u=d_{\max} f(\alpha)$, so optimal $\alphacued^\star$, $c_c^\star$, $c_u^\star$, $R^\star$, and CF match the symmetric baseline as real-number identities at every $(V, v, N, d'_{\max}, f_0, h, \rho)$; **Proposition prop:c5-sterbenz (bit-exact symmetric recovery on the validation configuration)** — $\beta(1)=\gamma(1)$ are the exact float $1.0$; Sterbenz lemma states $a/2 \le x \le 2a \Rightarrow \mathrm{fl}(x-a)=x-a$ exact; at the published validation config $a = d'_{\mathrm{base}} = 1.5$, the swept range $x \in [1.0, 2.0] \subset [0.75, 3.0] = [a/2, 2a]$ so round-trip is exact at every grid point; **scope clause** with closed-form off-band threshold eq:c5-sterbenz-threshold ($f_0 < h(1/N)/(1+h(1/N)) = 1/3$ at $h=\sqrt{\cdot}, N=4$) — the literal $0.0$ is a structural guarantee of the chosen config, not a property of the model; universal statement is "identical to machine precision"; **$\Rsens=1$ is the smooth centre paragraph** — slopes $d\beta/dr|_1=1/2$, $d\gamma/dr|_1=-1/2$; reviewer's continuity probe at $r=1\pm\epsilon$ scales linearly with slope $\approx 0.084$ reward units per unit shift; $r=1$ is the balanced gain-modulation-vs-suppression null, not a removable singularity; **C5 conservation-form-invariance paragraph** cross-references the §extensions-A3 corollary (closes the cross-link from the C5 side); **reproducibility paragraph** cites recovery-test pinned numerics at $\Rsens=1$ (VDA=0.039825, CF=0.728228, $R_{P1}=2.317239$ with max-abs-diff $0.0$ against reviewer logs; binary $P_{\mathrm{no-fa}}(\rho=0) \equiv$ product Eq. eq:pnofa-indep), sha256 `d3c62215…` (rb-001 recovery), sha256 `f4f57a89…` (rb-015 conservation-family invariance), and the two relevant test names. 2 new bib entries added to refs.bib (Sterbenz1974 *Floating-Point Computation*; Goldberg1991 ACM Computing Surveys, the standard secondary reference for the Sterbenz lemma in CS literature). Both cited by full bibliographic reference per the math-methods scope inherited from rb-008/rb-017 (Slepian 1962, Tong 1990, HLP1934). Build: 4-pass pdflatex+bibtex clean (pass 1 (1 fatal double-subscript on `\alphacued^\star_{\mathrm{asym}}` because `\alphacued` already expands to `\alpha_c`; fixed by reformulating with `\alphacued^\star\!\big|_{\mathrm{asym}, r=1}` evaluation-bar notation) → pass 1 retry → bibtex (added Sterbenz1974, Goldberg1991) → pass 2 (2 undefined cites) → bibtex 2 → pass 3 (cites settled) → pass 4 (refs settled, 0 undefined references); 35 pages / 2,165,118 bytes (was 33 / 2,152,496 at rb-017 = +2 pages, +12.6 KB — light-touch increment as scoped, no new sim).** |

## Load-bearing assumptions

| id | reviewer label (live) | rebuilt strength | backing |
| --- | --- | --- | --- |
| A1 — per-location SDT independence | CONTESTED | **Promoted to model parameter `rho`** (equicorrelated Gaussian, exact 1-D quadrature). Three levers, not two: criterion + sensitivity + decorrelation. §5.5 "upper bound on VDA" retracted; what independence actually upper-bounds is the *criterion fraction* (variant A; variant B is flat — reported as a sensitivity, not as a uniform claim). The pointwise upper-bound on VDA fails at the headline cell: max excess over the rho=0 curve grows from +4.84e-3 (rho=0.1) to +1.01e-2 (rho=0.4), sign-flip r ~ 0.38-0.56. The sign-flip generalises beyond the headline cell: across the `(V, v)` plane (rb-010 sweep, variant A), `ρ=0.2` suppresses VDA in 37.2% of cells at `r=0.3` and amplifies in 27.2% (cost-dominant regime ⇒ mostly suppression); the ratio inverts at `r=1` (52.8% amp / 17.5% supp) and `r=3` (54.0% amp / 16.1% supp) — the `r ≈ 0.4–0.6` headline-cell sign-flip locus organises the entire `(V, v)` plane, with strongest amplification at `(V≈0.7, v=10)` lifting a near-zero `r=0.3, ρ=0` cell (`VDA=0.0007`) to `VDA=0.0676` at `ρ=0.2`. | `Rebuild/model/core.py` (`p_no_fa_grid`, `p_no_fa_point`, `policies`); `Rebuild/model/tests/test_recovery.py` (rho->0 recovery, 7/7 PASS, sha256 `d3c62215...`); `Rebuild/sims/A1--rho-channel/` (rb-002, VDA(r, rho) and CF(rho) at the headline cell, both variants, sha256 `b692c064...`, figures rendered); **cell-wise sign-flip Δ-distribution: `Rebuild/sims/A1--vda-signflip-cellwise/` (rb-025, RB-025, pre-embed sha256 `489c7c25…`) — consumer of rb-003's results.json (no new model evaluation); joins ρ ∈ {0, 0.2} panels on (variant, r, V, v) to produce ΔVDA per cell across the 4,410-cell sweep; per-variant distribution (A: 18.3% amp / 28.2% supp / 53.5% inac, max amp +0.0497, max supp −0.0091; B: 12.2% amp / 27.5% supp / 60.3% inac, max amp +0.0058, max supp −0.0032); r-stratified crossover (variant A r ≈ 0.7943, variant B never crosses) parallel to rb-003's CF Δ-distribution; (V, r) signed-mean heatmap at v=5 cell-wise companion to rb-010's iso_vda_drho; three figures vda_delta_distribution / signflip_by_r / vda_sign_heatmap_v5; three structural-recovery contracts PASS (source-payload sha, sign-flip at nearest cell V=0.5125 to rb-002 headline V=0.5, cell-wise crossover 0.7943 ≥ rb-002 headline 0.4642)**; derivation: `Rebuild/derivations/A1--rho-channel.md` (rb-008, RB-003) — 7 sections (locus of A1 Eq. 1.4, exact 1-D reduction Eq. 2.3, ρ→0 recovery Eq. 2.4, Slepian monotonicity Prop. 3.1 + per-policy bounds Cor. 3.2, two-channel sign-decomposition §4, Statements A and B §4.3, numerical realisation §5 tied to sha256 b692c064…, scope §6); **manuscript drafted: `Rebuild/manuscript/sections/model.tex` §model (rb-009, RB-004): 5 subsections (sec:model-inherited compact SDT skeleton with Eqs. sdt-marginal, expected-reward, vda-def, cf-def-model; sec:model-booking with Eq. pnofa-indep isolating A1 to the single product, plus a remark separating A1 from a pooled-statistic A6 relaxation; sec:model-rho-channel with boxed Eq. pnofa-rho and the recovery contract Eq. rho-zero-recovery tied to sha256 d3c62215…; sec:model-three-levers with Definition def:three-levers naming the three independent mechanisms (criterion shift, sensitivity re-allocation, decorrelation); sec:model-upper-bound with the explicit retraction of the inherited §5.5 sentence — pointwise VDA excess +4.84e-3 → +1.01e-2 across ρ=0.1→0.4, sign-flip r ∈ [0.38, 0.56] variant A and r ≈ 0.26 variant B, CF monotone-down in ρ at headline cell variant A and cell-wise 84% one-sided in variant A / 64% in variant B across the rb-003 sweep, blockquote of the corrected version of the inherited §5.5 sentence); 3 figures includegraphicsd (vda_curves_variantA.png, vda_curves_variantB.png, cf_vs_rho.png) copied from `Rebuild/sims/A1--rho-channel/output/figures/`; cites Slepian1962, CohenMaunsell2009, RuffCohen2016, Srinath2021 (latter two added to refs.bib this run); PDF 16 pages / 1177200 bytes**. **Manuscript fold-in of cell-wise sign-flip: `Rebuild/manuscript/sections/model.tex` §sec:model-upper-bound (rb-026, RB-041) — ≈155 LaTeX lines appended after the corrected §5.5 blockquote: (a) cell-wise generalisation paragraph reporting rb-025 Δ-distribution at ρ = 0.2 (variant A: 18.3% amp / 28.2% supp / 53.5% inac, max amp +4.97e-2 = 5.3× rb-002 headline-cell max; variant B: 12.2% amp / 27.5% supp / 60.3% inac, max amp +5.77e-3); (b) Table tab:a1cw-summary (11 rows × 2 variant: fraction counts, ΔVDA min/q5/q50/q95/max/mean, cell-wise crossover R^×); (c) two-statement paragraph licensing (i) cell-wise crossover R^×(A) ≈ 0.794, variant B never crosses, with nearest-cell sweep at V=0.5125 reproducing the rb-002 small-r/large-r pattern; (ii) spatial structure at v=5 as cell-wise companion to the C3 thread's iso-ΔVDA figure (sec:results-c3); (d) three new figures includegraphicsd at 0.86\\linewidth: fig:a1cw-delta-distribution, fig:a1cw-signflip-by-r, fig:a1cw-sign-heatmap-v5 (copied with a1cw_ prefix from `Rebuild/sims/A1--vda-signflip-cellwise/output/figures/`). Two outdated "queued" references updated in-place: closed-form sign-flip locus is now cited as sec:appendix-deriv-c2 / Proposition prop:r-dagger-rho / Eq. eq:r-dagger-rho (folded in at rb-024, no longer queued); finer-ρ grid extension remains queued as RB-023; analytic Slepian-gradient locus for the cell-wise ∂VDA/∂ρ surface queued as RB-040. 0 new bib entries (purely internal cross-references); 0 new wiki stubs; audit.py not re-run (no wiki writes). Build 4-pass clean: 45 pages / 2,587,742 bytes (was 43/2,413,318 at rb-024 = +2 pages, +174,424 bytes). Passes 3 and 4 byte-identical — settled. The A1 row is now fully wired across sim (rb-002, rb-025), derivation (rb-008), manuscript-model (rb-009, rb-026), and appendix (rb-008 → §appendix-deriv-a1, plus cross-pin via §appendix-deriv-c2 of the C2 thread at rb-024).** Inherited from `Critique/derivations/A1--correlated-fa-upper-bound.md` and `Critique/replications/A1--correlated-fa/`. |
| A2 — single global r | CONFIRMED-CONDITIONAL | **Between-preparation reading adopted explicitly**: one effective r per fixed preparation. Heterogeneous per-location r_i presented as a model extension. **Model wiring (rb-019, RB-014):** the heterogeneous-r d'-map is now wired into the rebuilt model module as `d_prime_hetero(alloc, r_vec, d_max, f0, h, N, p)`, returning an N-vector of per-location $d'_i = \max(d_{\mathrm{base}} + s_i (d'_{\max} f(a_i) - d_{\mathrm{base}}), 0)$ with $s_i = \beta(r_i, p)$ if $a_i \ge 1/N$ else $\gamma(r_i, p)$. The conservation order $p$ propagates through `beta_gamma(r_i, p)` at each location, so heterogeneous-r and the A3 conservation family compose. Homogeneous-r recovery contract: under uniform `r_vec` and the canonical homogeneous allocation `canonical_alloc(alpha, N)`, `d_prime_hetero` returns `[d_c, d_u, d_u, ..., d_u]` byte-for-byte identical to `d_prime_asym(alpha, r, ...)` across 5,904 sampled cells (5,436 on alpha ≥ 1/N + 468 on the alpha < 1/N inversion regime), `h ∈ {sqrt, linear}`, `p ∈ {0, 0.5, 1.0}`, `r ∈ {0.1, 0.3, 0.398, 1.0, 3.162, 10.0}`. Heterogeneous-r sign smoke (`spread = 0.3` around `r_cued = 0.398`): `d_prime_hetero` returns a non-uniform uncued $d'_i$ vector monotone-increasing in $r_i$, as predicted from $\gamma(r_i)$ being monotone-decreasing and the uncued departure $d'_{\max} f((1-\alpha)/(N-1)) - d_{\mathrm{base}}$ being negative on the loss branch. Downstream pipeline (`P_no_fa`, `optimal_R`, `policies`) is still scalar in `(d_c, d_u)`; the heterogeneous-r regime is now scored via the rb-020 (RB-017) general policy/optimiser `er_full_policy(alloc, valid, v, r_vec, cell)` which accepts an N-vector `r_vec` and threads `d_prime_hetero` directly into the joint no-FA integral (rho-aware) and the criterion optimisation — see the A8 row for the recovery contract. | `Rebuild/model/core.py` (`d_prime_hetero`, `canonical_alloc`; lines added after `d_prime_asym`); `Rebuild/model/tests/test_heterogeneous_r.py` (rb-019, RB-014) — **5/5 PASS, sha256 `0486921f8a4e253a1682ee0731e52c7de800bf6c3dbb1b5e8ff3200602454c7e`**: TEST 1 alpha≥1/N grid recovery (5,436 cells, max\|diff\|=0.0), TEST 2 alpha<1/N inversion-regime recovery (468 cells, max\|diff\|=0.0), TEST 3 scalar broadcast (72 cells, max\|diff\|=0.0), TEST 4 CR-048 headline-cell spread=0 (max\|diff\|=0.0), TEST 5 spread=0.3 sign smoke (predicted monotone ordering). Pre-existing recovery digests `d3c62215…` (A1) and `f4f57a89…` (A3) unchanged — extension is purely additive. Inherited from `Critique/derivations/A2xA8--heterogeneous-r-allocation.md` (reviewer's derivation) and `Critique/replications/A2xA8--heterogeneous-r/verify_heterogeneous_r.py` (the validated single-spread sanity at the headline cell). **Empirical band (rb-021, RB-018): `Rebuild/sims/A2--heterogeneous-r/` (pre-hash sha256 `22b183f942d6b1f8868848ec1143ab959afd78c72cd6d3704763eedf5713e615`, 27 s wall-clock).** Five-test sweep at headline cell + interior-α critic cell + variant-B C1 corner, scored through `er_full_policy` so ρ ∈ {0, 0.2} threads through every evaluation. **Recovery contract**: spread=0 vs `policies()` at every (ρ, r) of the 21-point r-grid × 2 ρ = 42 headline-cell cells passes at 1e-9 (max|ΔVDA| = 2.35e-10, max|ΔCF| = 2.98e-10). **Criticality**: at interior-α cost-dominant cell (V=0.5, v=2, r=0.3, α*=0.660), tangent-gradient norm on uncued simplex is exactly 0 at spread=0 (equal-split is a critical point) and 2.6e-3 (ρ=0) / 5.8e-3 (ρ=0.2) at spread=0.3 — ρ amplifies the A2 asymmetry ~2×. **Allocation deviation**: ΔR = R(simplex-opt) − R(equal) bounded by 1.5e-4 at the cost-dominant cell and 0 exactly at the benefit-dominant cell (V=0.5, v=5, r=0.4, α*=1.000) at every (spread, ρ); A1 ρ=0.2 *suppresses* the cost-dominant ΔR by ~50% (1.48e-4 → 7.47e-5 at spread=0.3). **C2 peak invariance**: across 8 (spread, ρ) panels at the headline cell (V=0.5, v=5), peak VDA varies by ≤ 1e-5 at fixed ρ (0.07972 → 0.07971 at ρ=0; 0.08130 → 0.08130 at ρ=0.2) and peak r* is fixed at 0.398; the ρ-channel offset (+0.00158) is itself spread-invariant — A1 and A2 act orthogonally at the C2 peak. **C1 corner**: at the rb-005 variant-B minimum-CF corner (V=0.25, v=4, r=10), spread=0.3 raises CF by +0.0015 (ρ=0) and +0.0016 (ρ=0.2) — corner *not* deepened by A2, regardless of ρ. Three figures: `vda_curves_spread.png` (8-curve C2 reframe headline), `vda_peak_band.png` (peak invariance panel), `cf_contested_corner.png` (variant-B corner). Headline empirical narrative: **within-display heterogeneity is a bounded perturbation on every headline C1/C2 number, and this remains true under A1 ρ; A2 and A1 levers compose orthogonally at the headline cells, with the one suggestive asymmetry of ρ-induced suppression of the cost-dominant allocation deviation.** **Manuscript drafted: `Rebuild/manuscript/sections/extensions.tex` §extensions-A2 (rb-022, RB-035) — appended after the §extensions-A3 reproducibility paragraph (the file is now §extensions / §extensions-A3 / §extensions-A2); contents: claim restatement at CONFIRMED-CONDITIONAL strength with 6 heterogeneity citations (McAdamsMaunsell1999, ReynoldsHeeger2009, Treue1999, Carrasco2011, GhoseMaunsell2002, Sani2017 — all from research_db papers stubs); heterogeneous-$\Rsens$ d$'$-map eq:d-prime-hetero + recovery contract notes (rb-019 byte-for-byte, rb-020 $\le 2.78\times 10^{-10}$); Table tab:a2-rb021-summary 5-test summary; 4 findings (criticality conditional on homogeneity; $\Delta R \le 1.48\times 10^{-4}$ cost-dominant + A1 $\sim$$50\%$ suppression + $\Delta R = 0$ benefit-dominant cued-absorption; C2 peak coordinates invariant under spread at fixed $\corr$ + orthogonal A1$\times$A2 composition; variant-B corner not deepened by spread); Figures fig:a2-vda-curves-spread, fig:a2-vda-peak-band, fig:a2-cf-contested-corner (3 figures copied from `Rebuild/sims/A2--heterogeneous-r/output/figures/` to `Rebuild/manuscript/figures/` with `a2_` prefix); scope paragraph deferring variant-B (RB-036), larger multiplicative spreads (RB-037), N-dim A8 sim (RB-021), closed-form $\Delta R$ scaling derivation; reproducibility paragraph citing rb-021 sha256 `22b183f9…`, rb-019 sha256 `0486921f…`, rb-020 sha256 `883ea15a…`, and the rb-021 embedded $\le 2.98\times 10^{-10}$ recovery contract. 6 new bib entries added to refs.bib (all wired to existing research_db stubs, no new wiki writes). Build: 4-pass pdflatex+bibtex clean (pass 1 hit `multirow.sty not found` in TeX Live 2026basic; fixed by restructuring the table column header to plain `\multicolumn{2}{c}` over a Test label row, no multirow required); 40 pages / 2,379,430 bytes (was 35/2,165,118 at rb-018 = +5 pages, +214 KB); 0 undefined refs, 0 LaTeX warnings.** |
| A3 — additive conservation `beta + gamma = 2` | CONTESTED | **General conservation family** parameterised so additive (`beta+gamma=2`) and multiplicative (`beta*gamma=1`) are special cases. Headline numbers reported as bands over the family. The rebuilt model implements this as the **power-mean conservation family** $M_p(\beta, \gamma) = 1$ with $\beta/\gamma = r$: $p=1$ is additive (paper), $p=0$ is multiplicative, $p=-1$ is harmonic, intermediate $p$ traces a smooth band. The asymmetry ratio $\beta/\gamma = r$ is invariant in $p$ by definition; the symmetric corner $\beta(1,p)=\gamma(1,p)=1$ is invariant in $p$ exactly (C5 conservation-form-invariance, a hard consequence of the construction). The conservation choice is a *model assumption*, not a derived statement. **Empirically banded by rb-016 (RB-019):** at the C2 headline cell, the conservation choice moves peak VDA at v = 5 by ≈14% (0.0830 at p=1 → 0.0951 at p=0) and peak r* by ≈6% (0.3758 → 0.3548); on the 4,410-cell C1 sweep, the conservation swap p = 1 → p = 0 produces ΔCF ≤ 0 in *every* valid cell, drives 191 cells from CF ≥ 0.5 to CF < 0.5 with 0 reverse flips, deepens the variant-B min from 0.3040 to 0.2309, and roughly doubles `frac<0.5` from 4.0% to 8.3% (combined). The C1 median CF moves only marginally (variant A 0.7552 → 0.7540; variant B 0.7682 → 0.7640) — **central tendency robust to conservation choice; tail shape is not**. The rebuilt manuscript reports both. | **Model wiring:** `Rebuild/model/core.py` (`beta_gamma(r, p)`; threaded through `d_prime_asym`, `_alpha_opt`, `HeadlineCell.cons_p`, `policies`); `Rebuild/model/tests/test_conservation_family.py` (rb-015, RB-015) — 14/14 PASS, sha256 `f4f57a89…`: family identities + additive p=1 byte-exact recovery + multiplicative p=0 6.3e-7 vs reviewer + symmetric-corner identity to FP. **Empirical band:** `Rebuild/sims/A3--conservation-band/` (rb-016, RB-019, sha256 `055bf4ec862c711f2c6a3fa0831e1373b806a84c5f5c1a6b13fd1d9d7a039a33`, 4.94 MB, ~36 s) — Block A 84-r × 5-v × 3-p VDA(r) sweep at headline cell + per-(v,p) peaks + closed-form K_c/K_u/r†(v); Block B 4,410-cell × 2-p CF sweep with per-cell ΔCF distribution and flip-count tally (191 / 0 matches verdict text exactly). Four recovery tests pass (TEST 1 rb-006 pins ≤ 5e-5; TEST 2 reviewer A3 ≤ 1e-5 / observed 6.1e-7 CF; TEST 3 r† p-invariance to 0.0 across p ∈ {0, 0.5, 1.0}; TEST 4 per-variant median CF |Δ| ≤ 4.1e-5 vs rb-003 logs). Four figures: vda_curves_pfamily_v5.png, vda_peak_band.png, cf_histogram_pfamily.png, delta_cf_distribution.png. Sim's headline numbers reproduce the reviewer's verdict-text Block-C1 prediction (191 flips, 0 reverse, min CF× = 0.231) on the full 4,410-cell scope. **Manuscript drafted: `Rebuild/manuscript/sections/extensions.tex` §extensions-A3 (rb-017, RB-034) — new section file inserted between §results and §limitations in `main.tex`; subsections: claim restatement at defensible strength (central-tendency robust, tail not robust); power-mean conservation family definition $M_p(\beta,\gamma) = 1, \beta/\gamma = r$ (eq:conservation-family, eq:beta-gamma-of-p); C2 conservation-family band on the peak (Table tab:a3-c2-peak-band 5-row v-family × 3-p block; +14% envelope on peak VDA at v = 5; Figure fig:a3-vda-curves-p-v5 with $\rdagger$ overlay); **Proposition 3.1 ($\rdagger(v)$ is conservation-form-invariant)** + one-paragraph proof from the uniform-attention-point collapse $d_c = d_u = d_{\mathrm{base}}$ (numerical witness: rb-016 TEST 3 K_c/K_u/r† identical to FP across p); Figure fig:a3-vda-peak-band (per-p peak markers clustered vertically above the $p$-invariant $\rdagger(v)$ trace); C1 conservation-family band on the 4,410-cell sweep (Table tab:a3-c1-cf-band 6-row variant × p block reporting min/q5/q50/q95/max/frac_<0.5/frac_<0.6, reproducing reviewer verdict-text 191 flips / 0 reverse / min CF× = 0.231 / 4.0% → 8.3% combined frac_<0.5); **Theorem 3.2 (per-cell $\Delta\CF \le 0$ monotonicity, empirical)**; Figures fig:a3-cf-hist-pfamily (4-panel variant × p histograms) and fig:a3-delta-cf-distribution (per-cell ΔCF distribution); **C5 conservation-form-invariance corollary** ($\beta(1, p) = \gamma(1, p) = 1$ at every $p$ from eq:beta-gamma-of-p); Scope paragraph deferring joint (p × ρ) sweep, RB-033 formal derivation, closed-form ΔCF ≤ 0, and variant-B ρ-flatness interaction; Reproducibility paragraph citing sha256 `055bf4ec…`, rb-015 model-test sha256 `f4f57a89…`, and all four rb-016 recovery tests (TEST 1 rb-006 pins ≤ 5e-5; TEST 2 reviewer A3 max 6.1e-7; TEST 3 r† p-invariance FP identity; TEST 4 per-variant median ≤ 4.1e-5). 4 figures copied from `Rebuild/sims/A3--conservation-band/output/figures/` to `Rebuild/manuscript/figures/`. 1 new bib entry added to refs.bib (HLP1934 — Hardy-Littlewood-Pólya 1934 *Inequalities*, by full bib reference; research_db stub deferred per the math-methods scope inherited from rb-008 / CR-035/CR-037). Build: 4-pass pdflatex+bibtex clean (pass 1 → bibtex (HLP1934 added) → pass 2 → pass 3 settled, 0 undefined refs); 33 pages / 2,152,496 bytes (was 27/1,814,626 at rb-013 = +6 pages, +338 KB). Companion appendix-derivation §appendix-derivation-A3 stub also placed in `sections/appendix.tex` (deferred to RB-033).** **Derivation:** `Rebuild/derivations/A3--power-mean-conservation.md` (rb-029, RB-033, ~50 KB / 1020 lines) is the formal A3 derivation in the rebuild's voice: §2 closed-form weights $\cost(\Rsens; p) = (2/(\Rsens^p+1))^{1/p}$, $\benefit = \Rsens\cost$ (Eq. 2.3 boxed); §3 **HLP power-mean monotonicity as a KL-divergence closed form** $\partial \ln \cost / \partial p = -(1/p^2) \DKL(\mathrm{Bern}(\theta_p) \| \mathrm{Bern}(1/2))$ (Eq. 3.3 boxed, $\theta_p = \Rsens^p/(\Rsens^p+1)$) with Corollary 3.1 (strict monotone-decreasing $\benefit, \cost$ in $p$ for $\Rsens \ne 1$); §4 Proposition 4.1 (symmetric-corner identity, full proof) + Corollary 4.2 (C5 conservation-form-invariance, full proof); §5 **full three-step proof** of Proposition 5.1 ($\rdagger(\val)$ $p$-invariance, promoting the rb-017 §extensions-A3 sketch); §6 d'-channel chain-rule analysis of $\partial \CF / \partial p$ via the envelope theorem — establishes $\partial \Rpthree/\partial p \equiv 0$ analytically and reduces $\partial \CF/\partial p$ sign to a competition between cued/uncued $\dprime$-gradients, declares the uniform closed-form proof of $\Delta\CF \le 0$ as **open**; §7 numerical realisation cites rb-015 and rb-016 sha256s; §8 scope. KL-form Eq. (3.3) cross-checked numerically by finite-difference across 7 $(\Rsens, p)$ pairs (LHS-RHS agreement $\le 1.5\times 10^{-10}$). Manuscript-appendix fold-in (RB-046, rb-046, 2026-05-30) discharges the §appendix-deriv-a3 stub: `Rebuild/manuscript/sections/appendix.tex` `sec:appendix-deriv-a3` is now a self-contained subsection (≈220 LaTeX lines / +27 KB / 55-page PDF / 2,790,640 bytes) mirroring §appendix-deriv-c2's structure (rb-024 fold-in pattern). Contents: (i) boxed closed-form weights Eq. eq:a3d-closed-form-weights (companion of Eq. eq:beta-gamma-of-p); (ii) HLP power-mean monotonicity in pointwise KL-divergence closed form Eq. eq:a3d-hlp-kl (boxed; finite-difference verified to $1.5\times10^{-10}$ at 7 test pairs); (iii) Proposition prop:a3d-symmetric-corner ($\benefit(1; p) = \cost(1; p) = 1$, full proof) + Corollary cor:a3d-c5-invariance (C5 conservation-form-invariance, full proof; cross-refed from §appendix-c5); (iv) **full three-step proof of Prop prop:r-dagger-invariance** (the rb-017 §extensions-A3 sketch promoted to a structural derivation); (v) d'-channel chain rule Eq. eq:a3d-chain-rule / eq:a3d-dprime-of-p + Proposition prop:a3d-rp3-invariance ($\partial \Rpthree/\partial p \equiv 0$ at $\alpha = 1/\Nloc$, full proof; the one analytic full-strength substatement) + open-conjecture statement of $\Delta\CF \le 0$. Cross-references in extensions.tex (3 sites) discharged: §intro RB-033 mention, Theorem thm:delta-cf-monotone parenthetical, §scope item (ii). New macro `\DKL` in main.tex; 4-pass clean build (0 undefined refs; 0 non-hyperref warnings). The A3 thread is now wired across all four output kinds for the first time of any claim in the rebuild: **model (rb-015), sim (rb-016), manuscript-extensions (rb-017), derivation (rb-029, RB-033), and manuscript-appendix (rb-046, RB-046)**. |
| A6 — homogeneous decision rule | WEAKLY-SUPPORTED (live; drift from §3 "OPEN/in-progress") | **Provisional**: attention-coupled decision noise treated as a candidate third lever the criterion-fraction metric currently mis-books to attention. Treatment deferred until A6 lands a final label. | not yet wired (queued RB-016, RB-020) |
| A8 — homogeneous uncued allocation | CONFIRMED-CONDITIONAL | **Conditional**: homogeneity is optimal conditional on A2 (single r) AND on the cued slot's value-weight dominance (live verdict §re-derivation: equal split is a critical point of the reward by S_{N-1} exchange symmetry; restricted Hessian λ = λ_HR + λ_noFA with λ_noFA ≤ 0 unconditionally by Φ log-concavity ⇒ λ < 0 for concave/linear h, so homogeneity is the local OPTIMUM, not just an assumption, on the smooth branch; the forced-uniform-budget kink is a cusp with one-sided slope sign(β−γ) = sign(r−1); the value-weight inequality w_c ≥ w_u under V ≥ 1/N, v ≥ 1 + the C4 location-count asymmetry drive α* → 1 fastest exactly in the accelerating-h / benefit-dominant regimes that wanted to concentrate, so the uncued budget B = 1 − α* → 0 and A8 never binds at headline cells — reviewer max |Δ R(uncon − homog)| = 1.4e-4 over all four h families). The N-dimensional heterogeneous-uncued policy space is the honest generalisation, and is now WIRED into the rebuilt model module. **Model wiring (rb-020, RB-017):** the rho-aware grouped-criterion optimiser is now exposed as `optimal_ER_general(d_vec, wu_vec, n_vec, CR, rho)` and the full-policy driver `er_full_policy(alloc, valid, v, r_vec, cell)` in `Rebuild/model/core.py` — `alloc` and `valid` are N-vectors on the simplex, `r_vec` can be a length-N vector or scalar broadcast, and `cell.rho` and `cell.cons_p` flow through the joint no-FA integral (one-factor Gauss-Hermite reduction, same `(_GH_Z, _GH_W)` machinery as `p_no_fa_grid`) and the `d_prime_hetero` d'-map respectively. Grouping by `(d_i, w_i u_i)` reduces the inner criterion problem from N-D to G-D with G ≤ N (homogeneous alloc + uniform validity + scalar r gives G = 2; truly symmetric cells G = 1); criterion optimisation is exact at grid resolution (1-D argmax for G = 1, full 2-D argmax for G = 2, multi-restart coordinate ascent from 2^G + 1 seeds for G ≥ 3, each restart sweep an exact 1-D-per-group argmax until criteria stop changing). Homogeneous-allocation recovery contract: under `canonical_alloc(alpha, N)` + `homogeneous_validity(V, N)` + scalar r broadcast + `cons_p = 1`, `er_full_policy.R` reproduces `optimal_R(d_c, d_u, ...)` to **1e-9 absolute** (4,530 cells × 2 variants × 2 rho values × 3 p values — empirical `max|d| ≈ 2.78e-10`); the gap is the structural wu-reconstruction error `(N-1)·((1-V)/(N-1)) - (1-V)` at ULP scale, six orders of magnitude tighter than any reported number. The lifted machinery composes with A1 (rho passes through the integrand), A2 (heterogeneous r_vec via `d_prime_hetero`), and A3 (conservation order p via `cell.cons_p`); the A8 sim (RB-021), the A2 sweep (RB-018), and any future heterogeneous-validity / anti-cue / value-weight study can now score policies directly against this driver. | `Rebuild/model/core.py` (`optimal_ER_general`, `er_full_policy`, `homogeneous_validity`, plus helpers `_hr_omf_grids`, `_omf_grid_correlated`; lines added after `slepian_curve`); `Rebuild/model/__init__.py` (re-exports `er_full_policy`, `homogeneous_validity`, `optimal_ER_general`); `Rebuild/model/tests/test_general_policy.py` (rb-020, RB-017) — **7/7 PASS, sha256 `883ea15af9fd069e04c05ff156d65f33a7d25278891092539c6441d2248c3d39`**: TEST 1 rho=0 recovery on full alpha grid × r × V × v × variant A (4,530 cells, max|d|=2.77e-10), TEST 2 rho=0.2 recovery on same grid (4,530 cells, max|d|=2.78e-10), TEST 3 variant B sanity (302 cells, max|d|=1.19e-10), TEST 4 conservation p ∈ {0, 0.5, 1.0} threading (453 cells total, max|d| ≤ 2.11e-10 across the family), TEST 5 grouping/method check (alpha != 1/N → G=2 grid_2d; alpha = 1/N but V ≠ 1/N → G=2 grid_2d since wu differs; fully symmetric → G=1 grid_1d), TEST 6 scalar r broadcast equivalence (186 cells, max|d|=0.0), TEST 7 G=4 multi-restart smoke check on heterogeneous validity (returns finite R, method "coord_ascent_multi_restart"). Pre-existing recovery digests `d3c62215…` (A1), `f4f57a89…` (A3), `0486921f…` (A2) all unchanged — extension is purely additive. Inherited from `Critique/replications/A8--heterogeneous-uncued/run.py` (CR-036, run-012; the grouped-criterion machinery), with the rho channel and conservation-family threading added in the rebuild's voice. **N-dim uncued sweep under ρ × p (rb-027, RB-021): `Rebuild/sims/A8--nd-uncued-sweep/` — deterministic sha256 `beb2aa879402e5c9f4c354a2c9f53a98c466d0989085dc8c78370be99dee290b` (sort-keys JSON dump with `wall_clock_seconds` omitted; verified byte-identical on rerun), ~72 s wall-clock.** Three blocks. **Part 1c** (24 panels: 6 cells × ρ ∈ {0, 0.2} × p ∈ {1, 0}) full-simplex (step 0.05) vs homogeneous-constrained (α step 0.005) optimum: **(ρ=0, p=1) recovery PASS** (0/6 binds, max dR = 6.82×10⁻⁴, replicates CR-036 Part 1c at the same threshold dR > 1×10⁻³ AND uncued_spread > 0.05); **NEW FINDING — A8 binds at (p=0, symm-stress-r10)**: at V=0.25 = 1/N, v=1, r=10, variant A, the multiplicative conservation panel gives dR = +2.79×10⁻³ (ρ=0) → **+3.68×10⁻³ (ρ=0.2)**, full-simplex optimum at (a_cued=0.5, uncued_spread=0.5) vs homogeneous (a_cued ≈ 0.05); **ρ-amplification of the binding = 32%** (3.68/2.79); the same cell at p=1 has dR = −1.38×10⁻³ < 0 (homogeneous wins to grid slack). **Part 1 curvature** (20 panels: 5 cells × ρ × p): R″(0) along [+1, +1, −2] **negative in 20/20 panels** — equal-split is a local max at the homogeneous α* regardless of (ρ, p), so the F2 binding above is a **non-local A8 binding** (homogeneous optimum locally stable but globally dominated). ρ-amplification of |R″(0)| at p=1 is small and non-uniform (mean ratio 1.048, max 1.135, one cell *suppresses* by 6%): the rb-021 A2 ~2× ρ-amplification of equal-split criticality residual **does NOT generalise to A8** — A1 and A8 compose more orthogonally than A1 and A2 do. **Part 2** anti-cued (Wang-Theeuwes link, r=0.398, v=1, V=0.40): monotone-decreasing a_anti* in w_anti AND a_anti* < a_rest* at both ρ ∈ {0, 0.2}; ρ=0.2 only weakly perturbs the gradient — graded suppression survives the correlation channel. Three figures: `a8_simplex_dr.png` (headline F2-binding bar chart), `a8_curvature.png` (R″(0) heatmap), `a8_anticued_suppression.png` (graded gradient). All four model-test digests unchanged after sim (rb-001 `d3c62215…`, rb-015 `f4f57a89…`, rb-019 `0486921f…`, rb-020 `883ea15a…`); the sim is a pure consumer of `er_full_policy`. **Strength refinement**: the row's headline "A8 is innocuous at the model's own optimum" remains correct under the inherited (ρ=0, p=1) regime — confirmed F1. The rebuild now licenses one new conditional: **at p=0 (multiplicative conservation), A8 *can* bind in the benefit-dominant high-r symmetric-stress corner (V=1/N, v=1, r=10, variant A), and ρ=0.2 amplifies the binding by ~32%.** A focused variant-B replication (RB-043) and a finer r-grid around r ∈ {5, 10, 20} to bracket the p=0 binding onset (RB-044) are queued as follow-ups. **§extensions-A8 manuscript subsection drafted (rb-028, RB-042): `Rebuild/manuscript/sections/extensions.tex` §extensions-A8.** The subsection states the lifted policy space via the §extensions-A2 d-prime-hetero eq:d-prime-hetero (uniform $\boldsymbol{\Rsens}$, unconstrained $\boldsymbol{\alpha}$); reports the rb-027 sim's five findings F1–F5 with Table tab:a8-rb027-summary (24-row Part 1c × p panel) + Table tab:a8-rho-curv-ratio (Part 1 curvature ρ-amplification) + Figures fig:a8-simplex-dr / fig:a8-curvature / fig:a8-anticued-suppression; licenses F1 (recovery PASS at (ρ=0, p=1) — 0/6 binds, structural reproduction of CR-036 Part 1c) alongside F2 (the new conditional — A8 binds at multiplicative conservation in symm-stress-r10, ρ-amplified 32%) + F3 (R″(0) < 0 in 20/20 panels — equal-split is a local max, F2 binding is non-local) + F4 (ρ × A8 mean |R″(0)| amplification ratio 1.048 — much weaker than the rb-021/A2 ~2× analogue, with one cell suppressing by 6%) + F5 (anti-cued graded suppression survives ρ=0.2 — Wang-Theeuwes link of CR-036 Part 2 generalises across A1 channel, with two behavioural citations); scope defers RB-043 (variant-B), RB-044 (finer r-grid), N>4 generalisation, and the closed-form A8-binding-onset locus (a future Slepian-style derivation, not queued); reproducibility paragraph cites the rb-027 deterministic sha256 `beb2aa87…`, the rb-020 N-dim-policy contract sha256 `883ea15a…`, and the three pre-existing recovery digests unchanged (rb-001 `d3c62215…`, rb-015 `f4f57a89…`, rb-019 `0486921f…`). Build: 4-pass pdflatex+bibtex clean — **51 pages / 2,763,376 bytes (was 49/2,587,742 at rb-026 = +2 pages, +175,634 bytes)**. The A8 row is now wired across model (rb-017 / rb-020), sim (rb-027), and manuscript-extensions (rb-028); the only remaining A8 threads are the optional variant-B replication (RB-043, low priority), the finer r-grid sharpening (RB-044, low priority), and the sim-boundary hardening at the model level (RB-045, no verdict-bearing claim). |
| A4 (no learning), A5 (transfer-function family), A7 (reward variants) | OPEN | Treat per the live ledger; if unattacked, the rebuild may keep the paper's original handling but flags each as an **explicit, scoped limitation**. | flagged for later |

## Drift from §3 of the prompt

- **A6**: §3 of `agents/paper_rebuilder_prompt.md` v0.2 calls A6
  "OPEN/in-progress" at authoring; the live verdict
  `Critique/verdicts/A6--heterogeneous-decision-rule.md` reads
  WEAKLY-SUPPORTED. Direction is unchanged (still pre-CONTESTED), so
  the rebuilt strength above is unchanged; flagging as
  `proposed_mission_change: true` for owner ratification at the next
  prompt revision.

## How to read this file

- **Reviewer label** tracks the live `Critique/verdicts/<id>.md` file's
  `current_label`. If a live label changes, this column changes here
  too in the next run.
- **Rebuilt strength** is the ceiling on what any manuscript section
  may claim. Stating a claim more strongly is a mission violation
  (`agents/paper_rebuilder_prompt.md` §3, §8).
- **Backing** is the path(s) to the artifact(s) — model code,
  simulation directory, derivation file, manuscript section — that
  support the rebuilt strength. "Not yet wired" means the increment
  exists in `REBUILD_BACKLOG.md` but has not been executed.
